{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": {},
    "build": {
      "dependsOn": ["^build"]
    },
    "deploy": {
      "dependsOn": ["^build"],
      "cache": false
    }
  }
}
